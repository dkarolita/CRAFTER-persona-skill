interface:
  display_name: "CRAFTER Persona"
  short_description: "Create CRAFTER-style persona documents."
  default_prompt: "Use the CRAFTER Persona skill to create a grounded persona using internal/external taxonomy factors, then produce the final result as a readable DOCX document."
